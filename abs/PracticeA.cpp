#include <iostream>
#include <string>

using namespace std;

int main() {
  cin.tie(0);
  ios::sync_with_stdio(false);

  int a, b, c;
  string s;

  cin >> a;
  cin >> b >> c;
  cin >> s;

  cout << (a + b + c) << endl;
  cout << s << endl;

  return 0;
}
